# ============================================================
# === Запуск через Docker (docker compose up --build) ========
# ============================================================
# Эти переменные использует контейнер PostgreSQL и docker-compose.yml.
# DATABASE_URL для приложения compose задаёт сам (хост = db), здесь менять не нужно.
POSTGRES_DB=streamdesk
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# ============================================================

# База данных PostgreSQL (для локального запуска БЕЗ Docker)
# Формат: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Спецсимволы в пароле (@, :, # и т.д.) закодируйте в URL, например %40 для @.
# Пример: postgresql://postgres:mypass@localhost:5432/streamdesk
# При запуске через Docker эта строка игнорируется (compose подставляет хост db).
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/DATABASE

# Порт сервера
PORT=5000

# --- Сетевой принтер этикеток TSC/TSPL ---
# TE310 из утилиты принтера: IP 10.90.109.120, RAW-печать через порт 9100.
LABEL_PRINTER_HOST=10.90.109.120
LABEL_PRINTER_PORT=9100
LABEL_PRINTER_DPI=300
LABEL_WIDTH_MM=40
LABEL_HEIGHT_MM=20
LABEL_GAP_MM=2

# Режим: development | production
NODE_ENV=development

# Секрет для подписи сессий. В production обязателен, строка должна быть длинной и случайной.
# Сгенерировать: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SESSION_SECRET=

# --- StreamDesk Agent: ПК/сервер/vMix heartbeat из .bat ---
# Задайте одинаковый токен здесь и в STREAMDESK_AGENT_TOKEN на агенте, если CRM доступна не только локально.
# В development без токена агент разрешен для удобного локального теста.
# STREAMDESK_AGENT_TOKEN=

# --- Деплой на сервер: для npm run deploy ---
# Заполните один раз: скрипт будет подключаться без дополнительных вопросов.
# SERVER_USER=ваш_пользователь
# SERVER_IP=194.58.112.174
# SERVER_PATH=/var/www/streamdesk

# В production обязательно отключить fallback-админа и задать SESSION_SECRET.
# ALLOW_FALLBACK_ADMIN=false

# --- Платформенный админ StreamDesk ---
# Это владелец всей платформы: видит компании, пользователей, графики, ошибки и обращения.
# Рекомендуется задать только один такой аккаунт.
# PLATFORM_ADMIN_USERNAME=owner
# PLATFORM_ADMIN_PASSWORD=change_me
# PLATFORM_ADMIN_EMAIL=owner@your-company.ru
# PLATFORM_ADMIN_NAME=Владелец платформы

# Если пока нет HTTPS, можно временно разрешить логин по HTTP и не-secure session cookie.
# Для боевого домена лучше оставить false и поставить nginx/caddy с SSL.
# ALLOW_INSECURE_LOGIN=false
# SESSION_COOKIE_SECURE=true

# --- HTTPS ---
# В production используйте HTTPS: задайте пути к сертификату и ключу или поднимите приложение за nginx/caddy с SSL.
# Если пути пустые, приложение работает по HTTP, но логин по HTTP в production будет запрещен.
# SSL_CERT_PATH=./certs/fullchain.pem
# SSL_KEY_PATH=./certs/privkey.pem

# --- YouGile: таск-менеджер ---
# Ключ API: настройки YouGile -> получить ключ.
# Без ключа таск-менеджер работает только локально.
# YOUGILE_API_KEY=
# Базовый URL API YouGile.
# YOUGILE_BASE_URL=https://ru.yougile.com/api-v2
# ID компании YouGile, если нужна внешняя синхронизация.
# YOUGILE_COMPANY_ID=
# ID колонки YouGile для новых задач из CRM.
# YOUGILE_DEFAULT_COLUMN_ID=

# --- Hugging Face / AI ---
# Токен: https://huggingface.co/settings/tokens, права Inference API.
# HUGGINGFACE_API_KEY=
# или HF_TOKEN=
# Owner-only AI для эксплуатации платформы, не для пользовательской вкладки ChatGPT.
# HF_OPS_QUICK_MODEL=Qwen/Qwen3-235B-A22B-Instruct-2507
# HF_OPS_DEEP_MODEL=deepseek-ai/DeepSeek-V3.2

# --- Клиент ---
# Пересоберите frontend после изменения: npm run build.
# Базовый URL API, если фронт и бэк на разных доменах. Иначе оставьте пустым.
# VITE_API_BASE=

# --- AI для смет ---
# OPENAI_API_KEY=
# OPENAI_ESTIMATE_MODEL=gpt-5.2
# HF_ESTIMATE_MODEL=openai/gpt-oss-20b
