# file:-зависимости (заглушка stubs/empty-canvas для @napi-rs/canvas) ставим копией, а не симлинком.
# Так package-lock.json остаётся консистентным и для `npm install`, и для `npm ci` (в т.ч. в Docker).
install-links=true
