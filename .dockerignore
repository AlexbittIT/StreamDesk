# Зависимости и сборка — ставятся/собираются внутри образа
node_modules
dist
migrations/meta

# Java-сборка — собирается внутри образа (Maven), в контекст не нужна
backend-java/target
backend-java/uploads

# Git и IDE
.git
.gitignore
.idea
.vscode

# Секреты — не должны попадать в образ
.env
.env.*
!.env.example

# Локальные скрипты запуска/деплоя (не нужны в контейнере)
*.bat
*.sh
deploy*.mjs
deploy*.config*

# StreamDesk Agent — раздаётся клиентам отдельно, веб-части в образе пока не нужен.
# (.bat и .sh агента уже исключены строками *.bat/*.sh выше; добавляем .ps1)
# ВНИМАНИЕ: пока агент исключён, скачивание агента из CRM работать не будет — проверим связь позже.
scripts/streamdesk-agent.ps1

# Прочее
*.log
attached_assets
certs